import 'package:flutter/material.dart';

class OrderCompletedCustomWidget extends StatelessWidget {
  const OrderCompletedCustomWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: const Text('Order Completed'));
  }
}