import 'package:flutter/material.dart';

class OrderInProgressCustomWidget extends StatelessWidget {
  const OrderInProgressCustomWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: const Text('Order In Progress'));
  }
}